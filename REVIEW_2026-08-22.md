# wiki-quiz 종합 점검 보고서 (2026-08-22)

> 지난 점검(`tests/e2e_go_live_checklist.md`) 이후 레포에 Gemini provider, `OUTLINE_DOCUMENT_ID`
> 단일 문서 크롤링, Discord/Google Chat 배달, 에러 본문 로깅 등이 추가됐다. 이번 점검은
> 코드 전체를 다시 읽고 직접 `pytest`를 재실행한 뒤, "지금 구성대로 쓰면 100% 무료인가"를
> 공식 문서 기준으로 재확인하고, 그 외 구멍을 정리한 결과다.

## 요약

- 코드를 다시 받아 `pytest` 전체 스위트를 재실행 — **21개 테스트 모두 통과** (discord/google_chat
  테스트 7개 포함, 이전 점검 시점보다 7개 늘어남).
- **가장 중요한 발견은 비용이 아니라 데이터 프라이버시다**: 기본값인 `QUIZ_PROVIDER=gemini`
  무료 티어는 회사 개발 위키 내용을 Google이 제품 개선에 사용할 수 있다는 조항이 있다 (1번 항목).
- "Discord로 지금처럼 쓰면 100% 무료냐"는 질문에는 **조건부로 그렇다** — 각 구성요소별로
  무료인 조건과 예외를 2번 항목에 근거(공식 문서)와 함께 정리했다.
- 그 외 코드 레벨 구멍 8건을 3번 항목에 정리했다. 이 중 README의 `DELIVERY_MODE` 표기 누락
  1건은 사소하고 명확한 오기라 이번 점검에서 **직접 수정 완료**(아래 3-6 참고). 나머지는
  판단·선택이 필요해 수정하지 않고 권고만 남겼다.

---

## 1. [최우선] Gemini 무료 티어로 위키 콘텐츠를 보내는 것의 데이터 프라이버시 문제

**문제 정의**: `quiz_generator.py`는 매일 위키 문서·첨부파일에서 뽑은 텍스트 조각(`sampled`
chunks, 기본 15개)을 그대로 LLM에 프롬프트로 보낸다. 기본 provider는 `gemini`이고,
`gemini-3.5-flash-lite`는 **무료 티어**로 쓰도록 설계돼 있다(README도 "무료 티어로 사용
가능해 기본으로 삼음"이라고 명시).

**근거**: Google Gemini API 공식 약관(`ai.google.dev/gemini-api/terms`)을 직접 확인한 결과 —

- **무료 티어(Unpaid Services)**: "Google uses the content you submit to the Services and
  any generated responses to provide, improve, and develop Google products and services"
  — 즉 제출한 프롬프트·응답이 제품/모델 개선에 쓰이고, **사람 리뷰어가 계정과 분리된
  상태로 읽고 주석을 달 수도 있다.**
- **유료 티어(Paid Services)**: "Google doesn't use your prompts...or responses to improve
  our products" — 학습에 쓰이지 않고, 로그도 30일만 보관(안전/컴플라이언스 목적).
- 예외: EEA/스위스/영국 거주자는 무료 티어에도 유료 티어 수준의 데이터 보호가 적용된다.

이 파이프라인이 다루는 콘텐츠는 "개발 위키" — 아키텍처, 내부 API, 설정값, 때로는 코드
스니펫까지 포함될 수 있는 내부 문서다. 기본 설정 그대로 두면 이런 내용이 매일 무료 티어로
Google에 전송되고, 회사 정책상 허용되지 않을 수 있는 방식(제품 개선용 학습 데이터,
사람 리뷰어 열람)으로 쓰일 수 있다.

**트레이드오프**

| 선택 | 비용 | 데이터 프라이버시 |
|---|---|---|
| 현재 기본값: `QUIZ_PROVIDER=gemini` (무료 티어) | $0 | Google이 학습/리뷰에 사용 가능 |
| `QUIZ_PROVIDER=claude` (Anthropic 유료 종량제) | 종량제(하루 3문제 워크로드는 사실상 무시할 수준, README 기준 Haiku 입력 $1/출력 $5 per MTok) | Anthropic API 상업 이용 약관상 별도 동의 없이는 학습에 사용 안 함이 원칙(계정별 데이터 처리 설정 재확인 권장) |
| Gemini **유료 티어**로 전환(과금 계정 연결, 모델은 동일하게 유지 가능) | 소량 과금 | 학습에 사용 안 함, 로그 30일만 보관 |

**권고**: 위키에 실제로 민감한 내용(내부 인증정보, 고객 정보, 미공개 사업 정보 등)이
있는지 먼저 판단해야 한다. 조금이라도 있다면 `QUIZ_PROVIDER=claude`로 바꾸거나 Gemini
쪽에 결제 계정을 연결해 유료 티어로 전환하는 걸 권한다. 이 결정은 코드 문제가 아니라
"회사 데이터를 어디까지 무료 서비스에 넘길 수 있는가"라는 정책 판단이라 사용자가
직접 결정해야 한다 — 이번 점검에서 대신 바꾸지 않았다.

Sources:
- [Gemini API Additional Terms of Service](https://ai.google.dev/gemini-api/terms)

---

## 2. "지금처럼 Discord 연동해서 쓰면 100% 무료인가" 검증

구성요소별로 공식 문서를 직접 확인했다. 결론부터: **현재 설정(Gemini 무료 티어 +
Discord + GitHub Actions)로는 사실상 무료로 돌릴 수 있지만, "100% 무조건 무료"는
정확한 표현이 아니다** — 아래 조건들이 붙는다.

| 구성요소 | 무료인가 | 조건/예외 |
|---|---|---|
| **Discord 웹훅 발송** | 무료 | 공식 개발자 문서에 웹훅 발송 자체에 대한 과금 언급이 없음. `content` 2,000자 제한은 코드가 이미 분할 처리(`discord.py`)로 대응함. 하루 1~3개 메시지 수준에서는 레이트리밋에 걸릴 가능성도 낮음(단, 정확한 초당/분당 한도는 공식 문서에 수치로 명시돼 있지 않았음). |
| **Google Chat 웹훅 발송** | 무료 | 공식 문서에 과금 언급 없음. 다만 "space당 초당 1req 공유 쿼터"가 있음(문제→정답 두 메시지를 연달아 보내는 현재 코드가 이 쿼터에 걸릴 가능성은 낮으나 0은 아님). `threadKey` 스레드 그룹화는 문서에서 정식 지원 확인함 — 코드 구현이 맞다. |
| **Gemini API (`gemini-3.5-flash-lite`)** | 무료 (무료 티어) | 결제 계정 연결 없이 사용 가능한 게 맞음(공식 가격표에서 "Free of charge" 확인). **다만 정확한 분당/일당 요청 한도(RPM/TPM/RPD)는 공식 가격표엔 없고 AI Studio의 [rate-limit 페이지](https://aistudio.google.com/rate-limit)에서 실측해야 함** — 위키가 크거나 실행이 잦으면 조용히 막힐 수 있다. |
| **GitHub Actions (매일 1회 크론)** | **저장소가 Public이면 무제한 무료. Private이면 GitHub Free 플랜 기준 월 2,000분까지 무료, 초과분부터 분당 과금.** | 이 레포는 내부 위키 API 키·웹훅 URL을 다루므로 Private일 가능성이 높다. 하루 1회, 소규모 위키 기준 실행 시간이 몇 분 이내일 것으로 예상되지만 **실측하지 않았다** — 월 2,000분 한도에 여유가 있는지는 실제 실행 로그(Actions 탭의 실행 시간)로 확인 필요. |
| **Outline 서버 자체 운영비** | 이 파이프라인이 유발하는 추가 비용은 아님 | 이미 운영 중인 `wiki.class.day` 인스턴스를 그대로 호출만 하는 구조. |

**정리**: "Gemini 무료 티어 + Discord + (Public이거나 월 2,000분 이내의 Private) GitHub
Actions"라는 지금 조합은 **금전적으로는 거의 확실히 무료**다. 다만 (a) Gemini 무료 티어의
실제 요청 한도는 아직 실측되지 않았고, (b) GitHub Actions Private 저장소는 무제한이
아니라 월 2,000분이라는 한도가 있으며, (c) 위 1번 항목의 데이터 프라이버시 비용은
"금전"이 아니라 "정보 노출 리스크"로 별도로 따져야 한다. "100%"라는 표현은 이 세 가지
조건을 빼면 정확하지 않다.

Sources:
- [Gemini API Pricing](https://ai.google.dev/gemini-api/docs/pricing)
- [Gemini API Rate Limits](https://ai.google.dev/gemini-api/docs/rate-limits)
- [About billing for GitHub Actions](https://docs.github.com/en/billing/managing-billing-for-your-products/managing-billing-for-github-actions/about-billing-for-github-actions)
- [Discord Developer Docs — Webhook Resource](https://docs.discord.com/developers/resources/webhook)
- [Google Chat — Webhooks quickstart](https://developers.google.com/workspace/chat/quickstart/webhooks)
- [Google Chat — Format messages](https://developers.google.com/workspace/chat/format-messages)

---

## 3. 코드 재점검 결과 (구멍·버그)

pytest 21/21 통과 자체는 문제없다. 아래는 테스트로는 못 잡는, 코드를 다시 읽으며 발견한
항목이다. 심각도는 "지금 그대로 실서비스에 돌렸을 때 실제로 문제가 될 가능성"으로 매겼다.

| # | 위치 | 심각도 | 문제 | 권고 |
|---|---|---|---|---|
| 3-1 | `quiz_generator.py::_generate_with_gemini` | 높음 | 안전 필터 등으로 Gemini가 응답을 차단하면 `response.candidates`가 빈 리스트이거나 `content.parts`가 `None`일 수 있는데, 코드가 바로 `response.candidates[0].content.parts`에 접근해 `IndexError`/`TypeError`로 죽는다. Claude 경로는 tool_use를 못 찾으면 명확한 `RuntimeError`로 처리하는데 Gemini 경로만 이 방어가 없어 두 provider 간 에러 처리가 비대칭이다. | `candidates`가 비었거나 `parts`가 없는 경우를 먼저 체크해서 "Gemini가 응답을 차단/생성 실패했다"는 명확한 예외로 바꿔줄 것. |
| 3-2 | `delivery/*.py` 전체 | 중간 | Outline 크롤링 단계(`outline_client.py`)에만 429 백오프가 있고, 배달 단계(Slack/Discord/Google Chat/Email/Outline 문서 생성) 5곳 전부 재시도 로직이 없다. 하루 1회뿐인 실행에서 배달 시점에 일시적 네트워크 오류나 429가 한 번만 나도 그날 퀴즈가 통째로 안 나간다. | 최소한 배달 단계에도 짧은 재시도(예: 2~3회, 지수 백오프) 정도는 추가 검토. |
| 3-3 | `.github/workflows/daily_quiz.yml` | 중간 | 파이프라인 실패 시 별도 알림 스텝이 없다. GitHub 기본 정책상 저장소 소유자에게 이메일이 가지만, 이메일을 놓치면 "며칠째 퀴즈가 안 옴"을 늦게 알아차리게 된다. | 실패 시에만 Slack/Discord로 짧은 실패 알림을 보내는 스텝을 워크플로 마지막에 `if: failure()`로 추가하는 걸 권장. |
| 3-4 | `tests/` 전체 | 중간 | `quiz_generator.py`(provider 분기, 에러 처리), `main.py`(오케스트레이션), `config.py`(필수값 검증 로직)에 대한 단위 테스트가 하나도 없다. 이 세 파일이 최근 가장 크게 바뀐 파일들이자 분기 로직이 가장 복잡한 파일들이라, 테스트 커버리지 공백이 가장 아쉬운 지점과 겹친다. | 최소한 "gemini/claude 각각 정상 케이스", "필수 env 누락 시 KeyError", "OUTLINE_DOCUMENT_ID 우선순위" 정도는 목킹 테스트로 잠가둘 것. |
| 3-5 | `requirements.txt` | 낮음 | `anthropic>=0.40.0`, `google-genai>=1.0.0` 등 하한선만 있고 상한이 없다. GitHub Actions는 매일 새로 `pip install`하므로, 두 SDK 중 하나가 breaking release를 내면 코드를 안 건드려도 다음날 크론이 조용히 깨질 수 있다. | 상한 버전 고정(`<x.y`) 또는 `pip freeze` 기반 lock 파일 도입 검토. |
| 3-6 | `README.md` "설정" 섹션 | 낮음(문서) | `DELIVERY_MODE` 값 목록이 `outline`/`slack`/`email`/`cli` 4개만 적혀 있고 실제로 지원하는 `discord`/`google_chat`이 빠져 있었다(바로 아래 줄엔 `DISCORD_WEBHOOK_URL`이 이미 언급되는데 정작 모드 목록엔 없는 불일치). | **이번 점검에서 직접 수정 완료** — `outline`/`slack`/`discord`/`google_chat`/`email`/`cli` 6개로 갱신함. |
| 3-7 | `config.py` | 낮음 | `SAMPLE_CHUNK_COUNT`, `QUESTION_COUNT`에 대한 값 검증이 없다(0이나 음수를 넣어도 그대로 통과). | 크게 위험하진 않지만 `int(...)` 이후 `> 0` 정도의 방어 한 줄 추가 검토. |
| 3-8 | `requirements.txt` | 낮음 | `# slack_sdk>=3.31.0` 주석이 남아 있는데 실제 slack 연동은 `httpx` 직접 호출로 구현돼 있어 이 줄은 죽은 참고문일 뿐이다. | 혼란 방지를 위해 정리 검토(기능상 문제는 없음). |

---

## 4. 지난 점검(`tests/e2e_go_live_checklist.md`)과의 관계

지난번 작성한 체크리스트는 `ANTHROPIC_API_KEY` 기준(Claude 전용)으로 짜여 있어서, 이번에
추가된 `GEMINI_API_KEY`/`QUIZ_PROVIDER`/`OUTLINE_DOCUMENT_ID`/Discord/Google Chat 관련
점검 항목이 빠져 있다. 그 문서에 있던 "Outline 레이트리밋 실측 필요", "API 키 발급 권한은
사람만 확인 가능" 같은 원칙은 여전히 유효하지만, 스모크 테스트 스크립트 자체는 지금
갱신이 필요한 상태다. 원하면 다음 턴에 그 문서를 지금 구성(Gemini 기본값·Discord/Google
Chat 포함)에 맞춰 갱신할 수 있다.

## 5. 결과 및 남은 위험 · 다음 단계

**이번 점검에서 한 일**
- 레포 전체 재확인(변경된 8개 파일 + 신규 2개 delivery 모듈 + 신규 2개 테스트 파일 포함)
- `pytest` 재실행해 21/21 통과 직접 확인
- Gemini/Discord/Google Chat/GitHub Actions 각각의 비용·데이터 정책을 공식 문서로 재검증
- README의 `DELIVERY_MODE` 표기 누락 1건 직접 수정

**남은 위험 (사람이 판단/실행해야 함)**
- 1번 항목: 위키에 민감 정보가 있는지, Gemini 무료 티어를 계속 쓸지 결정 — 가장 시급함
- GitHub Actions 저장소가 Private인지, 실제 월 실행 시간이 2,000분 한도 안에 있는지 실측
- Gemini 무료 티어의 실제 RPM/TPM/RPD 한도를 AI Studio 대시보드에서 확인
- `tests/e2e_go_live_checklist.md`를 지금 구성(Gemini 기본값 포함)으로 갱신할지 여부
- 3번 항목의 중간 심각도 이상 항목(3-1, 3-2, 3-3, 3-4)에 대한 코드 수정 여부 판단

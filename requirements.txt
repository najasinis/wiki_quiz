# 요구 사항: Python >= 3.10 (코드 전체에서 `X | None` 문법(PEP 604) 사용).
# macOS 기본 `python3`는 버전이 낮을 수 있으니 `python3 --version`으로 먼저 확인할 것.
# (레포 루트의 `.python-version` 참고 — pyenv 사용 시 자동으로 3.12를 잡아준다)

# 퀴즈 생성 LLM (QUIZ_PROVIDER로 선택)
google-genai>=1.0.0     # gemini (기본값, 무료 티어)
anthropic>=0.40.0       # claude (유료 종량제, QUIZ_PROVIDER=claude 일 때만 사용)

# 첨부파일 파싱
pdfplumber>=0.11.0      # PDF
python-docx>=1.1.0      # Word (.docx)
markdown-it-py>=3.0.0   # Markdown

# HTTP / 유틸
httpx>=0.27.0
python-dotenv>=1.0.1
tenacity>=8.5.0          # 429 백오프 재시도

# 전달 방식 (선택적으로 사용)
# slack_sdk>=3.31.0      # DELIVERY_MODE=slack 일 때 필요

# 테스트
pytest>=8.3.0

# Notion API 클라이언트
notion-client>=2.2.1

# Claude API
anthropic>=0.40.0

# 첨부파일 파싱
pdfplumber>=0.11.0      # PDF
python-docx>=1.1.0      # Word (.docx)
markdown-it-py>=3.0.0   # Markdown

# HTTP / 유틸
httpx>=0.27.0
python-dotenv>=1.0.1
tenacity>=8.5.0          # 429 백오프 재시도

# 전달 방식 (선택적으로 사용)
# slack_sdk>=3.31.0      # DELIVERY_MODE=slack 일 때 필요

# 테스트
pytest>=8.3.0
